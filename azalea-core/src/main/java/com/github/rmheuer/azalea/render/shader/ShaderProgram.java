package com.github.rmheuer.azalea.render.shader;

import com.github.rmheuer.azalea.utils.SafeCloseable;

/**
 * A shader that is used to shade polygons.
 */
public interface ShaderProgram extends SafeCloseable {
}
